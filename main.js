let wochentag = 0;
  console.log(wochentag);

while (wochentag <= 7 ) {

    if (wochentag === 1) {
        console.log(`Der ${wochentag}. Tag ist der Montag`);
    } else if (wochentag === 2) {
        console.log(`Der ${wochentag}. Tag ist der Dienstag`);
    } else if (wochentag === 3) {
        console.log(`Der ${wochentag}. Tag ist der Mittwoch`);
    } else if (wochentag === 4) {
        console.log(`Der ${wochentag}. Tag ist der Donnerstag`);
    } else if (wochentag === 5) {
        console.log(`Der ${wochentag}. Tag ist der Freitag`);
    } else if (wochentag === 6) {
        console.log(`Der ${wochentag}. Tag ist der Samstag`);
    } else if (wochentag === 7) {
        console.log(`Der ${wochentag}. Tag ist der Sonntag`);
    } else {
        console.log ("Falscher Wert")
    }
    wochentag++;
} 